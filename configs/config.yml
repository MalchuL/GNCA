data:
  IMG_PATH: data/sonic.png

log:
  TRAIN_OUTPUT_FOLDER: train_log
  INFER_OUTPUT_FOLDER: infer_log

model:
  CHANNEL_N: 16       # Number of CA state channels
  TARGET_PADDING: 0   # Number of pixels used to pad the target image border
  TARGET_SIZE: 96
  BATCH_SIZE: 8
  POOL_SIZE: 8096
  CELL_FIRE_RATE: 0.5
  ITER_NUMBER: (64, 86) # Min, Max
  EPOCHES: 30000

  TARGET_EMOJI: "🦎" #@param {type:"string"}

  EXPERIMENT_TYPE: "Persistent" #@param ["Growing", "Persistent", "Regenerating"]
  EXPERIMENT_MAP: {"Growing":0, "Persistent":1, "Regenerating":2}
  EXPERIMENT_N: 1

  USE_PATTERN_POOL: 1  #[0, 1, 1][EXPERIMENT_N]
  DAMAGE_N: 0 #[0, 0, 3][EXPERIMENT_N]  # Number of patterns to damage in a batch

